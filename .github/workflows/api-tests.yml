name: API regression checks

on:
  pull_request:
    branches: [main, master]
    paths:
      - app.py
      - utils.py
      - requirements.txt
      - build_assets.py
      - vercel.json
      - static/**
      - public/static/**
      - templates/**
      - tests/**
      - .github/workflows/api-tests.yml
  push:
    branches: [main, master]
    paths:
      - app.py
      - utils.py
      - requirements.txt
      - build_assets.py
      - vercel.json
      - static/**
      - public/static/**
      - templates/**
      - tests/**
      - .github/workflows/api-tests.yml

permissions:
  contents: read

concurrency:
  group: api-tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
          cache: pip
      - run: python -m pip install -r requirements.txt
      - run: python -B build_assets.py
      - run: git diff --exit-code -- public/static
      - run: python -B -m unittest discover -s tests -v
